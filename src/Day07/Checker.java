package Day07;

public class Checker { // Checker class start
    // 매개변수 o, 반환값 o -> boolean (매개변수)
    boolean isEven( int x ){
        if ( x % 2 == 0 ){
            return true;
        }else {
            return false;
        }
    }
} // Checker class end
